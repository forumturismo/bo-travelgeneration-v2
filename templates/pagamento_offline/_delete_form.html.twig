<form method="post" action="{{ path('app_pagamento_offline_delete', {'id': pagamento_offline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pagamento_offline.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
